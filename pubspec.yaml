name: flutter_advanced_calendar
description: A highly customizable, feature-rich calendar package with event management, recurrence, multi-day events, and multiple view types.
version: 0.0.1
homepage: https://github.com/yourusername/flutter_advanced_calendar

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
